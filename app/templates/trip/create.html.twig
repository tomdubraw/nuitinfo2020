{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(form) }}
        {{ form_rest(form) }}
        <input type="submit" class="btn btn-primary btn-block">
    {{ form_end(form) }}

{% endblock %}
